openapi: 3.0.1
info:
  version: 1.0.0
  title: My example project
paths:
  /balance:
    get:
      responses:
        405:
          description: Invalid input
  /transactions:
    post:
      responses:
        405:
          description: Invalid input
#  /users:
#    $ref: 'users/types.yml#/user'
